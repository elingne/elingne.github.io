# elingne SPA v13 재현 가이드

## v12 변경점
- 게시물 저장 버튼 연속 클릭을 잠가 동일 게시물이 두 번 생성되는 현상 방지
- 캐릭터/페어 게시물의 다중 이미지 업로드 목록을 드래그해 순서 변경
- 기존 게시물 이미지도 편집 화면에서 드래그해 순서 변경 후 저장
- ADMIN BGM 플레이리스트를 드래그해 순서 변경하면 Supabase `sort_order`에 즉시 저장
- 기존 SPA BGM 무중단 구조와 파비콘 설정 유지

## GitHub 적용
ZIP의 파일을 저장소 루트에 덮어씁니다. `spa-v11.js`는 삭제하고 `spa-v13.js`를 추가하세요.
Supabase DB 마이그레이션은 필요 없습니다. 기존 `sort_order` 컬럼을 사용합니다.


## v13 변경사항
- 게시물 이미지 드래그 손잡이를 작게 조정하고 캡션을 이미지 바로 아래에 배치
- BGM 순서 변경은 드래그 방식만 유지하고 ↑/↓ 버튼 제거
- 게시물 상세의 수정/닫기 버튼을 나란히 배치
